name: Skool Report (on demand)

on:
  workflow_dispatch:
    inputs:
      report:
        description: "Which Skool report to post"
        required: false
        type: choice
        default: "daily"
        options:
          - daily
          - monthly
      channel:
        description: "Slack channel ID to post to (default: SLACK_CHANNEL_ID)"
        required: false
        type: string
  # No schedule: this workflow exists for the Slack `/skools` command, which fires
  # it via the workflow_dispatch REST endpoint (through the Vercel listener). Same
  # tab as the sales report (gid 170384010); the Skool blocks live in cols L:O
  # (daily) and Q:T (monthly).

permissions:
  contents: read

jobs:
  skools:
    name: Skool Report (on demand)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"

      - name: Set up Python
        run: uv python install 3.11

      - name: Install dependencies
        run: uv sync --no-dev

      - name: Install Playwright Chromium (with system deps)
        run: |
          # GitHub's ubuntu-24.04 runner ships a Google Chrome apt repo that
          # intermittently returns "Hash Sum mismatch", making the apt-get update
          # inside `--with-deps` fail with exit 100. We use Playwright's bundled
          # Chromium (not Google Chrome), so drop that repo, then install with a
          # few retries to ride out transient apt mirror hiccups.
          sudo rm -f /etc/apt/sources.list.d/*google* 2>/dev/null || true
          for attempt in 1 2 3; do
            uv run playwright install --with-deps chromium && exit 0
            echo "::warning::playwright install attempt $attempt failed; retrying in 15s"
            sleep 15
          done
          echo "::error::playwright install failed after 3 attempts"
          exit 1

      - name: Write Google service-account credentials
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          printf '%s' "$GOOGLE_SERVICE_ACCOUNT_JSON" > "$RUNNER_TEMP/sa.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/sa.json" >> "$GITHUB_ENV"

      - name: Run on-demand Skool report
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          # /skools passes the channel it was invoked in; else SLACK_CHANNEL_ID.
          SLACK_CHANNEL_ID: ${{ inputs.channel || secrets.SLACK_CHANNEL_ID }}
          SPREADSHEET_ID: "16YnE82TVn1I02-ipLzHkEgTtNoZEsMzvxlEHRmZmnyo"
          GID: "170384010"
          ONLY_RANKED: "1"
          # Blue / dark-blue theme (Skool only; sales keeps the default dark_green).
          THEME: "dark_blue"
          REPORT: ${{ inputs.report || 'daily' }}
        run: |
          if [ "$REPORT" = "monthly" ]; then
            RANGE="Q1:T50"; TITLE="Monthly Skool Report"
          else
            RANGE="L1:O50"; TITLE="Daily Skool Report"
          fi
          uv run python scripts/snapshot.py \
            --spreadsheet-id "$SPREADSHEET_ID" \
            --gid "$GID" \
            --range "$RANGE" \
            --title "$TITLE" \
            --theme "$THEME" \
            --output slack
