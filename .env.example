# Path to your Google service-account JSON
GOOGLE_APPLICATION_CREDENTIALS=./secrets/service-account.json

# Tokens and signing secrets live in gitignored secrets/.env — not here.
# Copy the TOKEN / SECRET keys below into secrets/.env and fill them there.

# Slack bot token (xoxb-...) with files:write and chat:write scopes
SLACK_BOT_TOKEN=

# Dedicated Gold Slack app bot (https://api.slack.com/apps/A0C11GQ6ESE).
# Blank = SLACK_BOT_TOKEN.
GOLD_SLACK_BOT_TOKEN=
# Channel the Gold report posts to.
GOLD_CHANNEL_ID=C0ATW4FSK0X
# Test-only extra channel. Uncomment to also post there.
# GOLD_EXTRA_CHANNEL_ID=C0BFN82DDLN

# Slack channel ID (e.g. C0123ABCDEF), not the channel name
SLACK_CHANNEL_ID=C0B4XFQRQ4F

# Optional defaults for smoke testing
DEFAULT_SPREADSHEET_ID=16YnE82TVn1I02-ipLzHkEgTtNoZEsMzvxlEHRmZmnyo
DEFAULT_GID=170384010
DEFAULT_RANGE=B1:J25

# Playwright viewport (px)
VIEWPORT_WIDTH=1400
VIEWPORT_HEIGHT=900

# --- Approvals report: GET /reports/approvals ---
# Channel the daily "APPROVALS REPORT" is posted to. The bot must be a MEMBER of
# this channel. If blank, falls back to SLACK_CHANNEL_ID.
# Test-only channel. Uncomment to post approvals there.
# APPROVALS_CHANNEL_ID=C0BFN82DDLN
# Optional separate bot for the approvals report (xoxb-...). Blank = SLACK_BOT_TOKEN.
APPROVALS_SLACK_BOT_TOKEN=
# Source sheet (APPTRACK 3.0, tab APPS). Defaults are baked into the app; override only if it moves.
APPROVALS_SPREADSHEET_ID=1Tc8x72ys7_oWQnUm03LWa-KHgHMXmX2HGH7gz1-xC_c
APPROVALS_GID=1445226540
APPROVALS_RANGE=A1:L
# Time zone used to compute "today" when ?date= is not supplied.
REPORT_TZ=America/New_York
# 0-based column indices: date_approved,client,bank,amount,invoice_sent,rep  (B,E,G,I,K,L)
APPROVALS_COLS=1,4,6,8,10,11

# Approvals analysis bar charts (scripts/approvals_analysis.py)
# KPI SUMPRODUCT on Apptrack Raw: Date Approved (B) + Amount Approved For (H).
APPROVALS_ANALYSIS_SPREADSHEET_ID=16YnE82TVn1I02-ipLzHkEgTtNoZEsMzvxlEHRmZmnyo
APPROVALS_ANALYSIS_GID=1037418474
APPROVALS_ANALYSIS_RANGE=A1:H
APPROVALS_ANALYSIS_WEEKLY_WEEKS=6
APPROVALS_ANALYSIS_MONTHLY_MONTHS=6
